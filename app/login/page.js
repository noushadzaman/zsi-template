import FloatingImg from "@/components/login/FloatingImg";
import Form from "@/components/login/Form";

const page = () => {
    
  return (
    <section className="min-h-[60vh] flex items-center justify-center">
      <FloatingImg />
      <Form />
    </section>
  );
};

export default page;
